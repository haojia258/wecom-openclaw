# PR 队列收敛审计报告

> 日期: 2026-05-24 | 执行模式: 只读 | 状态: develop (clean)

---

## 一、仓库当前状态

| 指标 | 值 |
|------|-----|
| 当前分支 | `develop` |
| Working Tree | clean |
| develop ahead of main | 50 commits |
| main ahead of develop | 0 commits |
| open PR 总数 | 7 |

---

## 二、逐个 PR 审计

### PR #27 — Phase2-D: Worker Network Runtime 网络代理运行时

| 字段 | 值 |
|------|-----|
| 源分支 | `feature/worker-network-runtime` |
| 目标分支 | `develop` ✅ |
| 创建时间 | 2026-05-23 |
| 冲突 | 0 处冲突 ✅ |
| 文件数 | 5 文件 (4 added + 1 modified) |
| 测试 | 26 新 + 374 全量 = 全部通过 |

**修改文件:**
| 文件 | 操作 |
|------|------|
| `orchestrator/worker-proxy-config.js` | 新增 |
| `orchestrator/worker-network-policy.js` | 新增 |
| `orchestrator/tests/test-worker-proxy-config.js` | 新增 |
| `orchestrator/tests/test-worker-network-policy.js` | 新增 |
| `orchestrator/workers/openai-worker.js` | 修改 |

**建议:** ✅ **MERGE** — 唯一活跃且无冲突的 PR，优先合并。

---

### PR #26 — Phase2-D: Worker Network Runtime 网络代理运行时

| 字段 | 值 |
|------|-----|
| 源分支 | `feature/worker-network-runtime` |
| 目标分支 | `main` ❌ |
| 创建时间 | 2026-05-23 |
| 与 #27 对比 | **完全重复**（同源分支、同内容） |

**重复分析：**
- #26 和 #27 源分支相同 (`feature/worker-network-runtime`)
- 唯一区别：#26 → main，#27 → develop
- #26 是创建 #27 前错误地选了 main 为 base 的遗留 PR

**建议:** ❌ **CLOSE** — 重复 PR。工作流要求 feature/* → develop → main，应保留 #27。

---

### PR #19 — feat(orchestrator): deploy dynamic planner v0.2

| 字段 | 值 |
|------|-----|
| 源分支 | `feature/deploy-orchestrator-v0-2` |
| 目标分支 | `develop` |
| 创建时间 | 2026-05-22 |
| 分支落后 develop | 16 commits |
| 文件数 | 10 文件 (6 added + 4 modified) |

**已被取代分析：**
- PR #20（AI Orchestrator Runtime Core **v0.4**）已于 2026-05-22 合并入 develop
- v0.4 包含 9 模块、201 测试，完全覆盖并超越本 PR 的 v0.2 功能
- 本 PR 的 `orchestrator-core.js`、`audit-recorder.js`、`rollback-planner.js` 均在 v0.4 中有更新版本

**建议:** ❌ **CLOSE** — 被 PR #20 (v0.4) 取代，内容已过时。

---

### PR #18 — feat(skill): add /活动 command + NLP fallback routing

| 字段 | 值 |
|------|-----|
| 源分支 | `feature/activity-nlp` |
| 目标分支 | `main` ❌ |
| 创建时间 | 2026-05-22 |
| 分支落后 develop | 16 commits |
| 冲突 vs develop | 3 处冲突 |

**重复分析：**
- 目标分支应为 `develop`，当前错误地指向 `main`
- 核心文件 `apps/wecom-adapter/src/commands/activity.js` 和 `get-activity.skill.js` **已在 develop 中存在**
- 活动系统功能（/活动 /活动利润 /活动报名）早已通过其他 PR 合并入 develop
- PR 描述中提到"第 6 层 NLP fallback 路由"也已在 command-center.js v1.9 中实现

**建议:** ❌ **CLOSE** — 功能已存在于 develop，且目标分支错误。

---

### PR #6 — feat(ads): add ads analysis module with local rules

| 字段 | 值 |
|------|-----|
| 源分支 | `feature/ads-agent-v1` |
| 目标分支 | `develop` |
| 创建时间 | 2026-05-19 |
| 分支落后 develop | 49 commits |
| 冲突 vs develop | 2 处冲突 |
| 唯一文件 | 6 个 ads 模块文件 |

**已被取代分析：**
- 所有 6 个文件（`ads-analysis.js`, `ads-rules.js`, `ads-score-model.js`, `ads/index.js` 等）**已在 develop 中存在**
- PR #10（ads-agent v2）与后续 PR 已将 ads 模块更新版本合并入 develop
- 本地分支版本是基于非常旧的 develop 基础（落后 49 commits）

**建议:** ❌ **CLOSE** — 文件已存在于 develop，内容被后续 PR 覆盖。

---

### PR #4 — feat(ai): add ops analysis module with local fallback

| 字段 | 值 |
|------|-----|
| 源分支 | `feature/codex-ai-analysis-v1` |
| 目标分支 | `develop` |
| 创建时间 | 2026-05-19 |
| 分支落后 develop | 49 commits |
| 冲突 vs develop | 2 处冲突 |
| 唯一文件 | 2 个文件 (ops-analysis.js + index.js) |

**已被取代分析：**
- 所有文件（`ops-analysis.js`, `ai/index.js`）**已在 develop 中存在**
- PR #5/#8（AI 运营分析 + memory）已合并，包含更新版本的 ops-analysis 模块
- 本 PR 的 113 行初始版本已被后续 PR 大幅扩展

**建议:** ❌ **CLOSE** — 被 PR #5/#8 取代，内容已在 develop 中。

---

### PR #3 — chore: establish git workflow for multi-ai collaboration

| 字段 | 值 |
|------|-----|
| 源分支 | `develop` |
| 目标分支 | `main` |
| 创建时间 | 2026-05-18 |
| 冲突 vs main | 0 处冲突 |
| develop 领先 main | 50 commits |
| 文件数 | 30 文件 |

**分析：**
- 本 PR 是 develop → main 的同步 PR
- 包含 Git workflow 文档、PR 模板、.gitignore、.env.example 等基础设施
- develop 已领先 main 50 个 commit，此 PR 实际上是一次大规模同步
- 按照工作流 `feature/* → PR → develop → PR → main → tag → deploy`，此 PR 是正确的最终步骤

**建议:** ⏸️ **HOLD** — 等待 develop 稳定、所有 feature PR 合并后，再合并入 main。当前 develop 还有未合并的 #27 + 可能的新 PR。

---

## 三、汇总矩阵

| PR | 标题 | 源→目标 | 冲突 | 状态 | 建议 |
|----|------|---------|------|------|------|
| #27 | Worker Network Runtime | feature → **develop** | 0 | 活跃 | ✅ MERGE |
| #26 | Worker Network Runtime | feature → **main** | — | **重复 #27** | ❌ CLOSE |
| #19 | Orchestrator v0.2 | feature → develop | — | 被 #20 取代 | ❌ CLOSE |
| #18 | /活动 + NLP | feature → **main** | 3 | 已在 develop | ❌ CLOSE |
| #6 | ads analysis | feature → develop | 2 | 已在 develop | ❌ CLOSE |
| #4 | ops analysis | feature → develop | 2 | 已在 develop | ❌ CLOSE |
| #3 | git workflow | develop → main | 0 | 等待同步 | ⏸️ HOLD |

---

## 四、推荐合并顺序

```
Step 1: ❌ 关闭 #26, #19, #18, #6, #4 (5 个冗余 PR)
Step 2: ✅ 合并 #27 → develop (Phase2-D Worker Network Runtime)
Step 3: ⏸️ 等待 develop 稳定 (确认生产环境部署 + 测试通过)
Step 4: ✅ 合并 #3 → main (develop → main 最终同步)
Step 5: 🏷️  打 tag + 生产部署
```

---

## 五、推荐关闭的重复/冗余 PR

| PR | 原因 |
|----|------|
| #26 | 与 #27 完全重复，目标分支错误 (应为 develop) |
| #19 | Orchestrator v0.2 被 PR #20 (v0.4) 取代 |
| #18 | 功能已存在，且目标分支错误 (应为 develop) |
| #6 | ads 模块已在 develop 中 |
| #4 | ops-analysis 模块已在 develop 中 |

---

## 六、合并前测试命令

```bash
# Step 1: PR #27 — Worker Network Runtime
cd /opt/wecom-openclaw
git checkout develop && git pull origin develop
git merge --no-ff feature/worker-network-runtime

# 运行全量测试
node apps/wecom-adapter/src/orchestrator/tests/test-worker-network-policy.js
node apps/wecom-adapter/src/orchestrator/tests/test-worker-proxy-config.js
node apps/wecom-adapter/src/orchestrator/tests/test-openai-worker.js

# Step 2: 部署验证
fuser -k 3001/tcp
pm2 delete wecom-adapter
pm2 start apps/wecom-adapter/src/index.js --name wecom-adapter
pm2 status | grep wecom-adapter
tail -20 /opt/wecom-openclaw/logs/app.log
```

---

## 七、风险点

| 风险 | 等级 | 说明 |
|------|------|------|
| 关闭 #4/#6 后回退困难 | 🟡 低 | 确认 develop 中的对应文件是完整且可用的版本 |
| #27 生产测试验证 | 🟡 低 | openai-worker.js 有修改，需确认代理配置有效性 |
| #18 关闭后可能丢失 NLP fallback | 🟢 无 | 功能已在 command-center.js v1.9 第 6 层中实现 |
| #3 合并入 main 时机 | 🟡 中 | 需确认 develop 上所有功能已在生产稳定运行 |
| Vault 污染未清理 | 🟡 中 | 已知问题，合并前不影响但建议尽快处理 |
